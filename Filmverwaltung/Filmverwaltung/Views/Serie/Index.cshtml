@model IEnumerable<Filmverwaltung.Models.Serie>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Genre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Laenge)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Produzent.Vorname)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Genre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Laenge)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Produzent.Vorname)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Serie }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Serie }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Serie })
        </td>
    </tr>
}

</table>
